import React from "react";
import "./App.css";

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <span onClick={() => location.reload()}>rudnam</span>
      </header>
      <main>
        <p>{"This doesn't really have anything right now."}</p>
        <p>工事中です。</p>
      </main>
    </div>
  );
}

export default App;
